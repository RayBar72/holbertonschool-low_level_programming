#include <stdio.h>
#include <string.h>
/**
 * main - program that prints its name, followed by a new line
 * @argc: int varibable of main
 * @argv: string
 * Return: 0
 */

int main(int argc, char **argv)
{

	(void)argc;
	puts(*argv);
	return (0);
}
