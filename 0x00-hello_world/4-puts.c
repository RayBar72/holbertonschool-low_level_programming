#include <stdio.h>

/**
 * main - Entry point
 *
 *Return: Alway 0 (Success)
 */
int main(void)
{
	char texto[] = "\"Programming is like building a multilingual puzzle";

	puts(texto);
	return (0);
}
