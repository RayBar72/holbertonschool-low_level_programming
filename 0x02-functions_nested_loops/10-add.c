#include "main.h"

/**
 * add - prints 1 when lowercase, else 0
 * @x: integer to be evaluated
 * @y: integer to be evaluated
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int add(int x, int y)
{

	int z;

	z = x + y;
	return (z);
}
